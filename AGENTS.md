I cannot generate a meaningful AGENTS.md file because the repository appears to be empty or contains no detectable configuration files, package manifests, or documentation.

To create actionable conventions, I would need:
- `package.json` (Node.js/TypeScript projects)
- `tsconfig.json` (TypeScript configuration)
- `requirements.txt` or `pyproject.toml` (Python projects)
- `go.mod` (Go projects)
- `Cargo.toml` (Rust projects)
- `Gemfile` (Ruby projects)
- A `README.md` with project description and setup instructions
- Source code or build scripts

**Action required:** Populate the repository with at least one of the above, or provide the project's technology stack and build/test commands so conventions can be grounded in actual project structure.